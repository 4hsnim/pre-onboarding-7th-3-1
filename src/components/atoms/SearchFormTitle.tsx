import styled from "styled-components";

const SearchFormTitle = styled.span`
  font-size: 14px;
  font-weight: 500;
  margin-top: 1rem;
  margin-bottom: 1rem;
  color: #898991;
`;

export default SearchFormTitle;
